# PMD baseline: static-analysis debt that predates the quality gate.
#
# Format: <rule>|<repository-relative path>|<count>
#
# This file is a ratchet. `./gradlew pmdBaselineVerification` fails when a file
# gains a violation, and equally when it loses one without the entry being
# removed here. Never add a line to make a new change pass; fix the violation.
#
# PreserveStackTrace: call sites raise a new exception inside a catch block
# using the caught exception's *cause* (or, for CustomLocalDateDeserializer,
# construct a new exception around it) rather than passing the caught
# exception itself - the real stack trace is still chained in, but PMD's
# static check cannot see through that indirection. Owned by #3 (Fiscal Data
# client) and #7 (HTTP contract).
PreserveStackTrace|src/main/java/com/pedrolima/wexchange/adapter/out/fiscal/HttpFiscalDataClient.java|1
PreserveStackTrace|src/main/java/com/pedrolima/wexchange/adapter/in/web/CustomLocalDateDeserializer.java|2
